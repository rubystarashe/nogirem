# 상세 버전 변경 기록

사용자용 요약은 [VERSION_HISTORY.md](VERSION_HISTORY.md)에서 확인할 수 있습니다.

## 0.3.0

- 녹화 설정의 `순환 저장 한도`를 `녹화 용량 한도`로 바꾸고 그 위에 분·초 단위 `최대 녹화 길이`를 추가했다. 기본 1시간이며 Ring은 이 시간 범위 또는 용량 한도를 넘거나 디스크 여유가 1GB 미만이면 오래된 청크부터 삭제한다.
- 클립 출력 이름의 중복 검사와 이름 변경에서 사용하는 `access`의 `node:fs/promises` import가 빠져 저장 시 `ReferenceError`가 발생하던 문제를 수정했다. 누적 녹화 시간은 Ring 용량을 현재 설정 비트레이트로 나눈 추정값 대신 각 MP4의 Media Foundation 실제 presentation duration 합계와 작성 중 청크 길이로 계산한다.
- 클립 저장 전 조회 범위 전체를 하나의 임시 MP4로 먼저 remux해 `저장 중 1%`에서 장시간 멈추던 단계를 제거했다. 선택 구간을 원본 Ring 청크에서 직접 추출하고 시작점 이전 청크는 MP4 duration만 더해 건너뛰며 처리 청크마다 진행률을 전달한다. 빈 구간이 없는 1배속 저장은 staging과 두 번째 오디오 인코딩도 생략해 338개 청크의 3,300초 지점 10초 추출 시간이 약 6.8초에서 3.6초로 감소했다.
- 제공된 `1번째 클립.mp4`는 영상 1,412.914초보다 오디오가 1,413.758초로 0.844초 길었고 packet 역전·50ms 이상 공백은 없었다. 청크별 PCM 마지막 sample이 해당 영상 청크 끝을 넘어도 다음 청크 뒤에 계속 붙어 초과분이 누적되던 원인을 확인해, PCM buffer를 영상 끝의 정확한 sample frame에서 잘라 기록한다. 같은 Ring의 1,400초 검증 출력은 영상 1,399.999초·오디오 1,400.002초로 차이가 약 3.5ms이며 DTS 역전과 50ms 이상 공백은 0건이다.
- 빈 타임라인 구간에서 검은 `gap-preview`가 영상 내부 영역만 덮어 하단에 직전 청크의 마지막 프레임이 남던 문제를 수정했다. embedded·일반·전체화면 미리보기 모두 검은 레이어가 전체 영역을 덮는다.
- 시작·주기 갱신을 포함한 모든 DXVK 런타임 확인 완료 시 메인 렌더러에 상태 이벤트를 보내고, 늦게 도착한 `checking` 응답이 이미 확정된 최신·업데이트 필요 상태를 덮어쓰지 않도록 변경
- DXVK 확인 중은 회색 새로고침과 `DXVK 확인 중`, 확인 실패는 주황 경고와 `DXVK 상태 확인 불가`로 분리해 초록색 새로고침 아이콘이 나타나지 않도록 수정
- 메인 화면 블랙박스 누적 길이에서 `블박` 접두사를 제거하고 녹화가 켜진 동안 글자 대비와 굵기를 높임
- 꺼진 편집 화면의 실패한 준비 Promise를 폐기해 `블랙박스 켜기` 후 재시도할 수 있게 하고, 블랙박스를 켠 채 게임을 나중에 실행한 경우에도 관리 창의 녹화 시작 상태를 받아 기존 Ring 청크를 자동으로 다시 불러오도록 변경
- 영상 추출의 빈 구간 처리와 재생속도를 클립 저장 설정 모달로 이동했다. 재생 청크 경계의 미세 오차를 빈 구간으로 오인하지 않도록 네이티브가 명시한 gap이 선택 범위와 1초 이상 겹칠 때만 처리 옵션을 표시하고, 같은 모달에서 클립 이름을 입력하며 빈 입력은 다음 `n번째 클립` 이름을 사용한다.
- 저장된 클립 목록 머리글과 전역 단축키에서 이름 입력 모달을 열고 빈 입력은 기존 번호 다음의 `n번째 클립`으로 저장한다. 완료된 파일 이름 변경까지 기다려 목록을 갱신하고 삭제는 main process의 확인 모달을 통과한 경우에만 수행
- 관리 창의 공통 하단 작업 줄을 제거하고 설정 저장 버튼만 녹화 설정 페이지 오른쪽에 배치했다. 사용 상태 토글은 제거하고 녹화 설정은 외곽 카드 없는 구분선 기반 목록형으로 변경
- 녹화 설정 페이지는 720px 폭의 컴팩트 창으로 전환하고 설정명과 280px 입력을 왼쪽 480px 영역에 밀집 배치한다. 다른 페이지로 돌아가면 저장된 영상 추출 창 크기를 복원한다.
- 추출 영상 길이를 분·초 입력으로 변경하고 초과 초를 자동 환산한다. `+` 조회 버튼은 `+5분`과 5분 추가로 바꾸고 재생 정보는 버튼 바로 오른쪽, 녹색 추출 지점은 길이 입력 오른쪽에 배치하며 실행 중 문구는 `저장 중 n%`, 완료 후에는 `클립 저장하기`로 복원한다.
- 저장된 클립 페이지 재진입 시 숨겨진 미리보기의 이전 크기로 화면비를 계산하던 문제를 수정했다. 페이지 크기 복원과 두 번의 렌더링 frame 후 미리보기 기본 영역을 다시 측정해 클립 창 폭을 결정한다.
- 블랙박스 조회 범위는 마지막 녹화 청크가 아니라 편집 창을 연 현재 시각을 끝점으로 삼는다. 재생 가능한 segment가 하나도 없으면 빈 index 결과를 오류로 중단하지 않고 검은 미리보기 위에 `최근 n분 동안 녹화된 영상이 없습니다`를 표시하며, 파일명의 실제 녹화 시작 시각과 미디어 길이로 범위 교차 여부를 판정한다.
- 영상 추출 화면에서 키보드 왼쪽·오른쪽 방향키로 재생 위치를 5초씩 이동하고 Enter로 미리보기를 전체화면 전환한다. 관리 창에 포커스가 있어도 명령을 iframe으로 전달하며, 추출 구간 끝까지 재생하면 커서를 시작점으로 되돌리지 않고 끝점에서 정지한다.
- 녹화 설정에 빠른 클립 저장 단축키 입력을 추가했다. Ctrl·Alt·Shift와 일반 키 조합을 직접 눌러 저장하며 recorder 시작 시 저장된 Electron accelerator를 전역 등록하고 다른 프로그램과 충돌하면 설정 화면에 알린다.
- 메인 화면의 블랙박스 켜짐·꺼짐 제어 아래에 누적 녹화 길이를 작은 반투명 문구로 표시
- 추출 범위에 녹화 공백이 포함되면 무음 검은 화면을 유지하거나 공백을 제외하고 녹화 구간끼리 이어붙이는 방식을 선택할 수 있도록 추가
- 추출 MP4 재생속도를 0.5·0.75·1·1.25·1.5·2배 중 선택하고 압축 영상 timestamp와 PCM 오디오 sample을 같은 비율로 변환해 출력 길이에 반영
- 네이티브 compose 단계가 piece 준비와 최종 결합 진행률을 전달하고 `MP4 추출` 버튼 배경과 백분율로 실시간 표시
- 여러 media piece를 준비할 때 각 영상의 `piece.start`가 아닌 원본 트랙 끝부분의 오디오를 잘라 붙이던 범위 계산을 수정해 영상과 같은 시작 시각·길이의 PCM 오디오만 결합
- 제공된 163.35초 클립은 최종 영상·오디오 길이 차이가 약 40ms였지만, 추출 전 청크들의 디코딩 PCM 길이를 영상과 독립 누적해 실제 소리 내용의 선택 시점이 밀릴 수 있음을 확인했다. 오디오 선택 위치를 청크별 영상 `fileStart`와 검증된 PCM 상대 timestamp에 배치하고 범위 시작점도 영상과 동일하게 계산하도록 변경했다.
- 2초 영상·1.8초 구분 tone을 가진 20개 청크에서 30초 지점 추출을 검증했다. 수정 출력은 영상 청크와 일치하는 1150Hz 구간을 선택했고 AAC packet DTS 중복·역전은 모두 0건이었다.
- 영상 추출 창을 열 때 최근 15분을 하나의 MP4로 remux하던 선행 작업을 제거하고 Ring 메타데이터 인덱스와 청크별 전용 URL을 반환해 현재 재생 위치의 청크만 즉시 로드하도록 변경했으며, 실제 MP4 결합은 추출 요청 시 한 번만 지연 수행
- 블랙박스 인코더 큐가 찬 경우에도 다음 캡처 시각을 전진시켜 WGC 콜백이 게임 프레임마다 반복 호출되는 혼잡 증폭을 차단하고, 프레임 드롭 집계를 atomic으로 변경해 캡처 콜백의 상태 mutex 대기를 제거
- Ring 청크 전체 열거·정렬·파일 크기 조회를 매초·청크 확정마다 반복하지 않도록 시작 시 한 번 구성하는 증분 용량 인덱스로 교체하고 게시·삭제·전체 비우기 결과만 반영
- 녹화와 영상 준비·추출 helper를 게임 외 CPU 마스크에 시작 즉시 고정하고 프로세스 우선순위를 `Below Normal`로 통일했으며, D3D11 장치의 GPU 스레드 우선순위를 `-2`로 낮춰 게임 렌더링을 우선
- 하이브리드 CPU에서는 recorder helper를 입력·매크로 엔진용 P-core와 겹치지 않는 E-core에 우선 고정하고, 터보 키 helper도 사용자가 선택한 게임 코어 수에 맞춘 대체 P-core 마스크를 사용하도록 변경
- 녹화 시작 전에 선택 코덱의 Media Foundation 하드웨어 인코더를 확인해 소프트웨어 인코딩으로 게임 CPU를 점유할 가능성을 차단하고 CPU affinity·프로세스·GPU 우선순위 제한 실패를 숨기지 않도록 변경
- 프레임마다 반복하던 D3D11 video processor 입력 view 생성을 캡처 texture별 재사용으로 바꾸고 Ring 청크 회전을 4초에서 10초로 완화해 GPU 객체 생성과 인코더 재시작 빈도를 줄임
- 게임 창 검색은 500ms 주기로 제한하고 캡처 초기화 실패 시 최대 5초 지수 backoff를 적용했다. 클립 결합 thread는 `Lowest`, 별도 영상 준비·추출 helper는 Windows background mode로 실행해 게임 중 경합을 줄임
- 고급 기능에서 게임 블랙박스 `사용하기`를 누르면 기능과 녹화를 함께 활성화하고 메인 화면으로 즉시 복귀하며, 녹화가 꺼진 상태의 영상 추출 화면에는 오류 문구 아래 `블랙박스 켜기` 버튼을 제공
- 블루스크린·강제 종료 중 NUL 데이터로 손상된 `affinity/game-core-setting.json` 등 영구 JSON 설정을 감지하면 파일을 폐기하고 기본값으로 자동 복구해 CPU 토폴로지 재검사를 계속하도록 변경
- 추출 길이를 `++`·`+` 조회 컨트롤과 같은 줄 오른쪽에 정렬하고, 녹화 길이·용량은 글자 크기를 12px로 키워 미리보기·MP4 추출 버튼과 같은 하단 작업 줄에 배치
- 영상이 없는 타임라인 공백도 추출 범위를 자유롭게 이동·조절할 수 있도록 제한을 제거하고 미리보기에서는 검은 화면, 최종 MP4에서는 무음 검은 프레임으로 합성
- 추출 범위를 녹화 segment와 공백 piece로 분할하고, Media Foundation으로 기준 영상과 같은 해상도·코덱의 검은 MP4를 생성한 뒤 녹화 piece와 결합하는 compose 모드 추가
- 영상 추출 타임라인을 편집 패널 좌우 전체 폭으로 확장하고 하단 왼쪽에 `++`·`+` 순서의 가로 버튼, 중앙에 재생 정보, 오른쪽에 추출 길이를 배치
- 연속 Ring 청크를 각각 별도 편집 구간으로 반환해 추출 길이가 청크 하나로 제한되던 회귀를 수정하고 1.5초 이하로 이어진 청크를 하나의 연속 segment로 병합
- 블랙박스 조회 범위를 실제 시각 기준으로 구성하고 앱 종료·재시작 등으로 1.5초 넘게 끊긴 구간은 회색 공백으로 표시하면서 앞뒤 녹화 구간은 각각 탐색·범위 이동·크기 조절·추출 가능하도록 가상 시간축과 미디어 시간 변환 추가
- 공백을 압축한 재생용 MP4와 실제 시각 타임라인 사이의 구간 메타데이터를 recorder helper가 반환하고 추출 요청은 선택한 녹화 구간의 미디어 좌표로 변환해 전달
- 영상 추출 트랙의 `+`·`++` 버튼을 타임라인 상단에 맞추고 누적 녹화 시간과 `현재 n.n / 최대 nGB` 용량을 시간·용량 순서의 가로 한 줄로 배치
- 블랙박스 영상 추출의 최초 조회 길이를 1분에서 15분으로 변경하고 `++` 직접 설정창 바깥을 누르면 입력을 적용하지 않고 닫도록 개선
- Affinity helper의 캐시된 간소화 상태가 미적용인 동안 메인 화면의 500ms 런타임 조회마다 최신 MUO를 직접 확인해 실제 적용 직후 상태가 반영되도록 변경
- 실시간 부스트 중단을 감시만 멈추는 일시정지에서 Affinity 전체 원복과 메모리 감시 종료로 변경하고 `부스트 중단중`·`부스트 적용 중단됨` 상태 문구 적용
- CPU 재정렬의 전체 완료 대기를 10초에서 45초로 늘리고 PowerShell 프로세스 조회에는 10초 상한을 적용해 지연과 실제 실패를 구분
- 패스트핑 적용 전 기본 경로의 실제 어댑터와 기존 레지스트리 값을 저장하고 VPN·가상 어댑터 또는 활성 타사 네트워크 필터가 감지되면 설정 변경을 차단
- 패스트핑 적용과 어댑터 재시작 후 유효 IPv4·기본 경로·게이트웨이 설정·DNS·Microsoft/GitHub HTTPS를 최대 30초 재검사하며, 실패하면 저장한 두 레지스트리 값을 자동 복원하고 어댑터를 다시 시작한 뒤 안내 모달 표시
- 앱을 시작할 때 활성 패스트핑 원본 기록이 있으면 같은 연결 검사를 실행한다. 반복 실패 시 자동 복원하고 성공한 수동·자동 복원은 원본 기록을 삭제해 이후 불필요한 검사를 중단
- CPU 종류와 특정 코어 조합을 예외 처리하지 않고 `P-core 절반`과 `4코어` 중 큰 값을 선택하되 백그라운드용 P-core 1개를 남기는 공통 기본 배정 규칙 적용
- 고급 기능에 `마비노기 CPU 우선 점유 비율 설정`을 추가해 가로형 숫자 버튼으로 게임 코어 수를 저장하고, 백그라운드용 P-core와 게임에서 제외되는 E-core는 `선택 불가 코어`로 함께 표시
- 선택한 개수까지의 버튼을 누적 강조하고 1개에 가까운 녹색부터 최대 개수의 적색까지 단계별 색상을 적용하며, 모든 선택·미선택·선택 불가 코어가 전체 행을 같은 비율로 채우도록 구성
- 코어 색상을 `1개 빨강 → 기본값 초록 → 최대값 빨강`의 양방향 단계로 바꾸고 다른 프로그램 성능 저하 가능성을 셀렉터 아래에 안내
- 고급 기능 항목 간격을 1.5배로 확대하고 코어 수 적용 중에는 셀렉터 전체를 반투명 비활성화하며 성공 문구 없이 오류만 하단에 표시
- 버그 리포트에 `제출하기` 버튼을 추가하고 허용된 Google Forms 주소를 시스템 브라우저로 여는 전용 IPC 연결
- 고급 기능에서 마비노기 CPU 우선 점유 설정을 터보 키보다 위에 배치
- 하이브리드 CPU의 선택 범위는 E-core가 게임에 배정되지 않도록 P-core 수를 기준으로 제한하며, 설정 변경 시 NIC 상태를 유지한 채 실행 중인 Affinity helper가 즉시 새 마스크를 적용
- 메인 화면 오른쪽 끝에 블랙박스 활성화 상태를 표시하는 텍스트 진입 버튼을 배치하고 고급 기능의 기존 항목을 제거
- 활성 상태는 노란 배경과 검은 글자, 비활성 상태는 투명 배경과 검은 글자를 사용하며 상태 변경 시 검은 가림막이 좌우로 전환되는 텍스트 애니메이션 적용
- 버튼 전체 활성 배경과 전환 가림막이 중복되던 구조를 제거하고 최종 상태 문구와 전환 문구에만 내용 너비의 배경을 적용
- 상태 문구를 `블랙박스 켜짐`·`블랙박스 꺼짐`으로 단순화하고 큰 상태 버튼은 기존 설정을 보존한 채 즉시 활성 상태만 변경하도록 전용 IPC 연결
- 상태 버튼 오른쪽에 별도 새 창 아이콘을 배치해 블랙박스 관리 창 열기 동작과 켜기·끄기 동작을 분리
- 텍스트와 아이콘 마스크를 선형 속도로 바꾸고 아이콘 시작을 텍스트 완료 30ms 전으로 당겨 경계에서 감속·정지하지 않는 연속 전환 적용
- 블랙박스 켜짐 상태의 텍스트와 관리 아이콘을 노란 배경과 검은 전경색으로 반전하고 전환 마스크는 대비되는 검은색으로 유지
- 상태 버튼의 남는 고정 폭과 아이콘 사이 간격을 제거
- 켜기·끄기 처리 중 전역 disabled 투명도를 적용하지 않고 현재 문구를 텍스트 없는 검은 가림막으로 완전히 덮은 채 helper 처리가 끝날 때까지 정지
- helper 상태 변경이 완료된 뒤 가림막 아래의 최종 문구·색상을 교체하고 가림막을 좌우로 걷어내는 동일한 양방향 전환 적용
- 메인 버튼의 켜기·끄기 요청 중 1초 상태 폴링이 변경 전 helper 상태를 다시 반영해 전환을 역방향으로 재시작하던 경쟁 상태를 차단
- 메인 블랙박스 상태 버튼과 새 창 아이콘의 크기를 약 10% 확대
- 배포 직전 적용했던 블랙박스 가용성 잠금을 해제하고 고급 기능의 `사용하기`·`사용 중` 전환과 메인 화면 제어 및 recorder helper 자동 실행을 다시 활성화
- 기능 사용을 해제하면 실행 중인 recorder helper를 종료하되 코덱·화질·용량·클립 길이 설정은 보존하며, 다시 사용할 때 녹화는 꺼진 상태로 시작
- 기존 설정에 기능 사용 여부가 없더라도 녹화가 켜져 있으면 사용 중으로 이관하고, 관리 창 설정 저장에서도 기능 사용 상태를 보존
- Vulkan 업데이트 관리와 같은 일반 보조 `BrowserWindow`에 설정·실행·클립 저장·영상 추출·저장 폴더·순환 녹화 삭제 제어를 통합하고 항상 위 속성은 사용하지 않음
- 블랙박스 관리 창 preload와 IPC는 해당 창에서만 호출할 수 있도록 sender를 검증하며 트레이 진입 시 다른 보조 창과 함께 안전하게 종료
- 관리 창 기본 페이지에 기존 고정 시점 영상 추출 편집기를 내장하고 상단 녹화·용량·오디오 상태, 중앙 영상 미리보기·타임라인, 하단 클립·폴더·비우기·녹화 제어가 유지되는 구조로 재편
- `영상 추출`·`저장된 클립`·`녹화 설정` 페이지를 분리하고 관리 창을 가변 1040×760 크기로 확대
- `Clips` 폴더의 MP4를 수정 시각 내림차순으로 조회해 날짜별로 묶고 시간·용량과 함께 표시하며 선택한 영상을 관리 창 안에서 재생하는 제한 프로토콜과 IPC 추가
- 내장 편집기는 iframe의 직접 Electron 권한 없이 허용된 네 가지 편집 요청만 부모 관리 창의 제한 preload에 전달하며, 관리 창 종료 시 임시 편집 세션을 정리
- MP4 presentation duration 메타데이터로 각 청크 길이와 디코딩 시작 청크를 계산해 편집 트랙 생성 전 전체 영상 sample을 두 번 순회하던 과정을 제거하고 기본 최근 60초 유지
- 실제 Ring 청크 2,109개 환경에서 156MB·58.91초 편집 트랙을 약 886ms에 생성하고 ffprobe 재생 정보를 검증
- 타임라인 전체에서 클릭 즉시 재생 위치를 이동하고 선택 가이드 가운데 드래그는 범위 이동, 양쪽 끝 드래그는 시작·종료점과 추출 길이 조절로 분리
- 첫 트랙의 추출 범위를 최근 최대 30초로 다시 초기화해 1초 범위로 축소되는 상태를 복구
- 재생 버튼과 영상 클릭 및 스페이스 입력을 하나의 재생·일시정지 함수로 통합하고 브라우저 재생 실패 사유를 편집 화면에 표시
- 상단 상태 카드 3개를 제거하고 타이틀 오른쪽에 녹색 점과 `녹화 중`, 화면 우측 끝에 사용 용량과 누적 길이만 표시
- 편집·저장 클립 프로토콜에 단일 byte-range 요청을 처리하는 `206 Partial Content`, `Content-Range`, `Accept-Ranges` 응답을 구현해 대용량 MP4 전체 로드 전에도 재생·시간 이동 지원
- 각 4초 MP4가 별도 AAC 인코더를 시작하며 만드는 첫 1024-sample priming frame을 청크 결합 시 제거하고 이후 오디오 timestamp를 한 frame 당겨 주기적인 약 20ms 무음 제거
- 기존 60초 편집 트랙에서 약 4초마다 반복되던 20~27ms 디지털 무음과 수정 트랙의 연속 오디오 packet timestamp를 ffmpeg·ffprobe로 비교 검증
- 편집 창을 열 때 작성 중인 4초 청크를 강제로 확정하던 대기를 제거하고 최신 확정 청크 종료 시점을 기준으로 즉시 트랙 생성 시작
- 지속 녹화 helper는 `Below Normal`을 유지하되 사용자가 요청한 트랙 생성·추출 utility는 `Normal` 우선순위로 분리해 녹화 중 60초 트랙 준비를 약 6.6초에서 1.4초로 단축
- 추출 범위 안에서 재생을 시작하거나 재생 중 범위 안을 클릭하면 범위 끝에서 정지하도록 변경
- 추출 범위 좌우 경계에 14px 크기 조절 hit area를 추가해 마우스를 올리면 좌우 화살표 커서를 표시
- 추출 범위 끝에 도달해 자동 정지하면 playhead와 재생 시간을 범위 시작점으로 복귀하도록 변경
- 정상 재생 가능한 청크 간 시각 차이를 녹화 실패로 잘못 표시하던 회색 구간 추정을 제거
- 제공된 30초 추출 파일에서 clipping이나 AAC packet 누락 대신 필터 디코딩 시 오디오 timestamp가 반복 역전·중복되는 현상을 확인
- 청크별 AAC frame 삭제·직접 결합을 제거하고 각 청크를 PCM으로 디코딩한 뒤 연속 sample 시간축으로 AAC를 한 번 다시 생성해 음 깨짐 수정
- 수정 helper로 30초 트랙과 20초 추출 파일을 생성해 오디오 길이 20.000초, 일반·필터 디코딩 timestamp 경고 0건을 확인
- `전체 트랙 길이`를 `블랙박스 조회 길이`로 변경하고 분·초 입력을 분리했으며, 초 입력이 60 이상이면 자동으로 분에 올려 `100초 → 1분 40초`처럼 정규화
- 추출 영상과 저장 클립의 원본 화면비를 읽어 미리보기 영역에 적용하고 현재 미리보기 높이를 기준으로 관리 창 너비를 조절해 21:9 영상은 16:9보다 넓게 표시
- 관리 창 크기 요청은 현재 디스플레이 작업 영역과 최소 900px 너비 안으로 제한하고 페이지 전환 시 해당 영상 화면비로 다시 맞춤
- 저장된 클립 페이지를 왼쪽 목록·오른쪽 영상 순서로 재배치하고 이름 클릭 인라인 변경, Windows 파일명 검증·중복 차단과 확인 후 삭제 지원
- 상단의 녹화 용량·누적시간을 제거하고 영상 추출 타임라인의 `+`·`++` 버튼 아래 상태 영역으로 이동
- 별도 `recorder-helper.exe`가 마비노기 `Client.exe` 창을 Windows Graphics Capture로 외부 캡처하며 게임 프로세스 주입이나 렌더링 hook은 사용하지 않음
- Windows 프로세스별 WASAPI loopback으로 `Client.exe`와 자식 프로세스의 출력 소리만 48kHz 스테레오 PCM으로 캡처하고 AAC 192kbps로 MP4에 기록
- 프로세스별 loopback 공식 지원 기준인 Windows 빌드 20348 이상에서 사용하며 미지원 환경은 영상 녹화를 유지하고 오디오 부분 실패를 표시
- 오디오 packet timestamp를 영상과 같은 QPC 기반 100ns 시간축에 맞추고 timestamp 오류·불연속·비정상 점프는 직전 sample 종료 시각으로 복구
- 오디오 캡처 thread에 Windows Audio MMCSS를 적용하고 대기열을 packet 개수가 아닌 최대 1초 분량 PCM frame으로 제한
- 녹화 세션 전체에서 유지되는 자동 게인을 적용해 목표 peak `-6dB`, 최대 증폭 `+24dB`, 상승 복귀 2.5초로 작은 게임 소리를 보정
- `-65dB` 이하 바닥 잡음은 증폭하지 않고 큰 효과음에는 즉시 gain을 낮추며 최종 `-1dB` hard limiter로 PCM clipping을 방지
- 현재 자동 보정 gain을 helper 상태 JSON에 기록하고 고급 기능의 블랙박스 실행 상태에서 실시간 dB 값으로 표시
- 다른 앱의 출력 소리와 마이크는 캡처 대상에서 제외하며 오디오 연결 실패 시 영상 녹화는 계속하고 상태 화면에 부분 실패를 표시
- 게임 실행 경로 판별 허용 목록에 `Mabinogi_Test` 폴더를 추가해 테스트 서버의 `Client.exe`도 Affinity·경로 공유·블랙박스 대상으로 인식
- D3D11 GPU 텍스처를 원본 비율의 NV12로 변환하고 Media Foundation 하드웨어 인코더에 전달해 GPU→CPU 화면 복사를 제거
- 기본 자동 화질은 메모리 16GB·논리 CPU 12개 이상이면 최대 1440p, 그 외 환경은 최대 1080p를 선택하며 원본보다 작은 화면은 확대하지 않음
- 설정 화면에서 자동·최대 1080p·최대 1440p·원본 화질을 직접 선택하고 실제 적용 화질과 화질별 예상 보존 시간을 표시
- 1440p 60fps 기준 H.264 24Mbps·HEVC 16Mbps로 화질을 높이고 1080p·원본 및 30fps에도 해상도별 비트레이트 프리셋 적용
- 캡처 대기열을 최대 3프레임으로 제한하고 인코딩이 밀리면 게임을 기다리게 하지 않고 녹화 프레임을 폐기
- 녹화 helper 프로세스와 인코딩 스레드는 `Below Normal` 우선순위로 실행하고 기존 Affinity의 일반 백그라운드 CPU 그룹 적용 대상에 포함
- 녹화 파일을 10초 MP4 청크로 확정하고 20·50·100·200GB 한도 또는 최소 디스크 여유 5GB·설정 용량 10% 기준을 넘으면 오래된 청크부터 삭제
- 클립 요청 시 현재 청크를 먼저 확정하고 최근 30·60·120초 청크를 같은 디스크의 hard link로 보호한 뒤 재인코딩 없이 단일 MP4로 결합
- 고급 기능 버튼과 전역 `Ctrl+Shift+F10` 단축키에서 클립 저장을 요청하며 완성 파일은 Windows 동영상 폴더의 `마비노기 렘 블랙박스/Clips`에 보관
- 고급 기능의 `영상 추출` 버튼은 누른 시점을 고정한 항상 위 편집 창을 열고 현재 녹화 청크를 즉시 확정해 최근 60초 트랙을 준비
- 편집 플레이바 왼쪽의 `+`로 동일 기준 시점의 이전 30초를 추가하고 `++` 입력으로 최대 6시간 범위에서 트랙 길이를 직접 지정
- 추출 영상 길이에 맞춘 가이드를 플레이바 위에 표시하며 포인터 드래그, 임의 시점 탐색과 선택 구간 미리보기를 지원
- 선택 구간은 이전 키프레임을 디코딩용 프리롤로 유지하면서 음수 타임스탬프를 사용해 표시 구간만 정확한 길이의 MP4로 무재인코딩 추출
- 트랙 생성과 추출은 실행 중인 녹화를 막지 않는 별도 helper 유틸리티 프로세스에서 처리하고 임시 편집 트랙은 창 종료 또는 교체 뒤 정리
- 편집 창은 `Esc`로 닫을 수 있으며 다른 창 위에 유지되고 preload가 편집 세션·트랙 길이·추출·파일 위치 IPC만 제한적으로 노출
- 편집 영상은 세션·트랙 토큰을 검증하는 `nogirem-blackbox` 전용 프로토콜로만 스트리밍해 renderer에 임의 `file://` 절대 경로를 노출하지 않음
- 압축 sample remux에서 converter를 비활성화하고 해상도·frame rate·codec sequence header가 일치하는 최신 연속 청크만 결합
- PTS와 함께 decode timestamp를 재기준화하고 실제 frame rate를 duration fallback에 사용하며 형식 변경·reader 오류는 손상 파일 대신 명시적 실패 처리
- 클립·편집 remux에서 AAC stream의 PTS·DTS와 1024-sample duration을 청크별로 재기준화해 게임 소리를 전체 트랙과 선택 구간에 유지
- 트랙과 클립은 `.partial.mp4`를 완성한 뒤 최종 이름으로 교체해 실패한 MP4가 정상 결과처럼 노출되지 않도록 보호
- 클립 저장·편집 flush·helper 종료 control 작업을 Electron에서 직렬화해 단일 control JSON을 서로 덮어쓰지 않도록 변경
- 목표 fps보다 빠른 WGC callback도 `TryGetNextFrame`으로 먼저 비워 frame pool이 정체되지 않도록 수정
- 청크별 실제 frame rate 분수의 미세한 차이를 영상 형식 변경으로 오판해 최근 저장이 마지막 0~3초 청크만 남기던 문제 수정
- 호환성 판정은 codec·해상도·sequence header를 사용하고 선택 청크의 실제 media duration에서 마지막 요청 시간만 정확히 잘라 저장
- 새 청크는 `.partial.mp4`로 기록하고 Media Foundation `Finalize()` 성공 후에만 정식 `.mp4`로 원자 변경해 클립 스레드가 작성 중인 파일을 선택하지 않도록 수정
- 시작 시 남은 미완성 청크를 제거하고 최신 청크가 손상됐으면 이전의 정상·호환 청크부터 선택해 바이트 스트림 형식 오류와 2초 클립을 방지
- 블랙박스 설정·상태·제어 IPC를 preload에 제한적으로 노출하고 앱 시작·업데이트·종료 시 helper 생명주기와 단축키를 함께 관리
- 공식 Microsoft C++/WinRT projection 생성기를 빌드 시 SHA-256 검증 후 사용하고 완성된 helper만 설치본의 `asarUnpack` 자산으로 포함
- 오디오 캡처는 Audio MMCSS thread에서 복사·enqueue만 수행하고 AAC 인코딩은 기존 Below Normal encoder thread에서 영상과 함께 4초 청크에 기록
- 고급 기능에 진단 로그 추출 버튼을 추가해 앱·Electron·Windows 버전, CPU·GPU·메모리, 업데이트·창·터보 키·블랙박스 상태와 AppData 로그·상태 파일을 단일 ZIP으로 저장
- 사용자 데이터 절대 경로, IP 주소, 이메일, 일반적인 token·비밀번호·cookie·서명값을 ZIP 생성 전에 마스킹
- 녹화 Ring·Clips, helper 실행 파일, Chromium cache는 제외하고 개별 파일 최근 5MB·전체 원본 25MB로 제한해 대용량 로그가 앱을 장시간 막지 않도록 보호
- 진단 ZIP 생성은 주 창에서만 호출 가능한 preload IPC로 제한하고 저장 완료 후 Windows 탐색기에서 결과 파일을 표시
- 진단 ZIP에서 불필요한 안내용 `README.txt`를 제거하고 시스템 요약·포함 파일 목록·관련 로그만 생성
- 4초 청크 경계의 SinkWriter `Finalize()`와 파일 게시를 별도 스레드로 옮겨 다음 청크 기록을 즉시 이어가고, 새 writer 준비가 대기열 범위를 넘긴 경우 시간 공백을 가진 프레임을 다음 청크에 섞지 않도록 보호
- 블랙박스 상태에 현재 순환 녹화의 예상 누적 시간을 용량과 함께 표시하고, 저장된 `Clips`는 유지한 채 `Ring` 청크만 전체 삭제하는 기능 추가
- 메모리 helper에 부모 앱 PID를 전달하고 1초 주기의 기존 작업 loop에서 부모 생존을 확인해 앱이 비정상 종료돼도 helper와 잠금 파일이 남지 않도록 수정
- 시작 시 잠금 소유자의 부모 앱이 사라진 기존 고아 helper를 발견하면 제어 파일로 정상 종료시킨 뒤 새 helper를 시작해 15초 후 실시간 부스트가 일시정지로 돌아가는 문제를 복구
- 패스트핑 최초 적용 전에 기본 경로 인터페이스의 GUID와 `TcpAckFrequency`·`TCPNoDelay` 존재 여부 및 값을 사용자 데이터에 보존
- 시스템 최적화 네트워크 카드에 설정 복원 버튼과 재연결 확인 모달을 추가하고, 저장 기록이 있으면 원래 값을 복원하며 기록이 없으면 두 레지스트리 값을 삭제해 Windows 기본값으로 전환
- 네트워크 복원은 TCP 자동 조정 수준을 변경하지 않고 현재 상태만 다시 조회하며, 저장된 인터페이스가 있으면 현재 기본 경로가 특수 네트워크여도 해당 원본을 복원

## 0.2.9

- 눌린 터보 키 순서를 유지해 가장 최근 설정 키만 반복하고, 해당 키를 떼면 계속 누르고 있던 직전 설정 키를 즉시 다시 활성화
- 설정되지 않은 일반 키의 누름·해제는 활성 터보 키와 입력 순서에 영향을 주지 않도록 keyboard hook 처리 변경
- modifier 입력 시 터보 반복을 중단하는 기존 안전 동작은 유지
- 자동 교체가 적용되도록 터보 키 helper 버전을 0.1.4로 갱신
- 개발 모드는 AppData 설치 복사본 대신 `native/turbo-key/bin/turbo-key-helper.exe`를 직접 실행해 로컬 재빌드 결과를 바로 테스트
- 고급 기능 위치 안내 말풍선은 hover와 무관하게 실제 화면에 표시될 때 횟수를 저장하고 최대 3회까지 노출
- 메인 렌더러의 Esc 입력은 종료 확인, 네트워크·Radeon 확인, 터보 키 약관·설정, 충돌 경고, 최적화 상태, 문서·고급 기능 화면 순으로 최상위 UI 하나만 취소
- Vulkan 업데이트 관리·DXVK 설정 안내·주변 캐릭터 간소화 보조 창은 Electron 입력 단계에서 Esc를 공통 처리해 창 닫기 동작을 일관되게 적용
- 전역 버튼의 `focus-visible` 초록색 outline을 제거해 Esc 등 키보드 입력 후 기존 버튼에 강조 테두리가 남지 않도록 변경
- Esc를 터보 키 UI·설정 정규화·Rust helper 허용 목록에서 모두 제외하고 기존 저장값에서도 자동 제거
- 선택 색상을 덮던 터보 키 키캡 hover 스타일을 제거하고 helper 자동 교체 버전을 0.1.5로 갱신

## 0.2.8

- 앱 내부 닫기 버튼은 창 복귀용 포커스 함수를 다시 호출하지 않고 현재 포커스를 유지한 채 종료 선택 화면을 표시
- 트레이 메뉴가 닫힌 뒤 `restore → show → focus` 순서로 창을 복원하고 일반 포커스가 실패할 때만 한 번 재시도하도록 변경
- 창 복원 시 `screen-saver` 최상단 설정과 `moveTop()`을 제거해 다른 창의 활성 상태를 과도하게 변경하지 않도록 개선
- 최초 포커스 직후 성공 여부와 무관하게 150ms 뒤 상태를 다시 확인해 UAC나 이전 창에 포커스를 빼앗긴 경우 한 번 복구
- 최소화를 기다리지 않고 Electron 준비 직후 트레이 아이콘을 항상 생성
- Chromium 렌더러가 `launch-failed`, `exitCode 18`로 생성되지 않으면 해당 실행에 한해 `--no-sandbox` 호환 모드로 한 번 자동 재실행
- 트레이 진입 시 투명 보조 창을 숨기는 대신 완전히 종료해 지연된 입력 가로채기 방지
- 트레이 복귀 시 메인 창을 다시 활성화하고 별도 포커스 재시도 없이 `show()` 한 번으로 표시
- 트레이 복귀 직후와 250ms 뒤 모든 BrowserWindow의 표시·포커스·활성화·최상단·위치 상태를 시작 로그에 기록

## 0.2.7

- 트레이 최소화 시 투명 보조 창을 숨기고 입력을 차단하며 복귀 시 메인 창의 마우스 입력과 포커스를 강제로 복원
- 트레이 종료는 렌더러 상태와 무관하게 조작할 수 있는 Windows 네이티브 확인창으로 변경
- 트레이 메뉴에서 종료할 때 메인 창이 열려 있으면 앱 종료 화면을 포커스하고, 숨겨져 있으면 메인 창 복원 없이 독립된 종료 선택 메시지박스만 표시
- 트레이에 숨긴 동안 업데이트가 시작돼도 투명 업데이트 레이어가 앱과 종료 모달의 클릭을 가로채지 않도록 수정
- 설치 완료 후 시작 메뉴 바로가기 대신 설치된 실행 파일을 직접 실행해 바로가기 누락·계정 경로 차이로 시작하지 못하는 문제 방지

## 0.2.6

- 실시간 부스트 상태 파일이 다른 프로세스에 잠겨도 파일 교체를 재시도하고 기존 파일을 안전하게 대체하도록 개선
- 일시적인 상태 기록 실패만으로 Affinity·메모리 helper가 종료되지 않고 다음 주기에 다시 기록하도록 개선
- 중복 helper가 같은 상태 파일과 CPU 설정을 동시에 처리하지 않도록 단일 실행 잠금 추가
- 앱과 터보 키가 `Mabinogi`, `마비노기`, `Nexon` 폴더 바로 아래의 `Client.exe`를 마비노기로 인식하도록 설치 경로 탐지 확장
- 그 외 폴더에서도 `Client.exe`와 같은 위치에 `Mabinogi.exe`가 있으면 마비노기 설치 경로로 인식하도록 보조 판정 추가

## 0.2.5

- 손상된 실시간 부스트 상태 파일 때문에 기능이 시작되지 않거나 조작되지 않던 문제를 자동 복구하도록 수정
- 특수 네트워크 인터페이스를 사용하는 PC방에서는 패스트핑을 안전하게 생략하고 적용 가능한 TCP 최적화는 계속 처리하도록 개선
- NVIDIA 설정 저장 시 `NVAPI -1`이 발생하면 새 세션으로 재시도하고, 계속 실패하는 항목만 별도로 표시하도록 개선
- 터보 키를 켤 때 적용 키 설정을 먼저 확인하고 사용 중일 때만 키 설정 버튼을 표시하도록 개선
- 터보 키 대기 중 발생하던 잦은 파일·프로세스 확인과 무결성 검사를 줄여 유휴 상태의 시스템 부하 개선
- 앱 업데이트 후 구버전 터보 키 실행 파일을 감지하면 기존 동의와 키 설정을 유지한 채 자동 교체하도록 개선

## 0.2.4

- 실시간 부스트 구성 요소가 일부만 실행된 경우 상태 전환이 되지 않던 문제 수정
- 실시간 부스트 일시정지 중에도 주변 캐릭터 강제 간소화 적용 상태를 정확히 표시하도록 개선

## 0.2.3

- 초기 상태 확인이나 시작 리소스 로드가 지연·실패해도 창을 표시하고, 시작 직후 상태와 배경색이 어긋나지 않도록 안정성 개선
- 문서 링크 초기화 오류로 검은 화면이 남던 문제와 렌더러 자동 복구 중 앱이 종료되던 문제 수정
- 터보 키 실행 파일을 설치본에서 분리하고 운영정책 확인 후 선택적으로 다운로드·제거하도록 변경

## 0.2.2

- PC 성능에 따른 터보 키 입력 속도 편차와 반복 지연 개선
- 업데이트 다운로드가 멈추거나 앱 화면이 응답하지 않을 때 자동 복구하도록 안정성 개선

## 0.2.1

- 고급 기능에 적용 키와 입력 간격을 직접 설정하는 마비노기 전용 터보 키 추가
- 터보 키와 주요 키보드·마우스 프로그램의 입력 엔진을 게임 외 P-core에 배치하도록 CPU 조정 개선

## 0.2.0

- Intel 하이브리드 CPU의 P/E 코어를 실제 토폴로지로 식별해 마비노기는 P-core 절반, 다른 프로그램은 나머지 P-core와 모든 E-core를 사용하도록 개선
- 같은 물리 코어의 SMT 스레드가 서로 다른 영역으로 나뉘지 않도록 CPU 배치 개선
- 마비노기 종료 시 다른 프로그램의 CPU 선호도를 원래 상태로 자동 복원
- 다른 드라이브에 설치된 마비노기 경로를 실행 중인 프로세스에서 자동 감지
- 업데이트 다운로드 진행률이 일시적으로 뒤로 돌아가는 표시 오류 수정
- 고급 기능에 Windows 로그인 시 창과 시작 음악 없이 트레이에서 자동 실행하는 옵션 추가
- 주요 키보드·마우스 프로그램의 입력 엔진을 CPU 조정 대상에서 제외
- 실제 CPU 선호도 적용 여부만으로 종료 확인 창을 판단하고 표시 지연 개선

## 0.1.4

- 일부 NVIDIA 드라이버에서 그래픽 설정 저장이 실패하는 오류 수정

## 0.1.3

- 앱 최소화 시 트레이 아이콘으로 이동
- 실시간 부스트 일시정지 상태에서 트레이 아이콘을 회색으로 표시

## 0.1.2

- 마비노기 설치 경로 확인 불가 버그 수정

## 0.1.1

- 프로그램 종료시 설정 되돌리기 버튼과 유지 버튼의 위치와 디자인 변경

## 0.1.0

- 공개 사용자 버전 릴리즈
- 비정상 종료 후 실행 시 무반응 버그 수정

## 0.0.3

- 자동 업데이트 기능 추가

## 0.0.2

- Radeon 그래픽 최적화 기능 추가
- 개발자 문서 영역 추가
- 개발자용 CPU 재정렬 기능 추가

## 0.0.1

- 테스트 버전 개발
