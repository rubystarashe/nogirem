# 약관에 위배되지 않는 작동 방식

마비노기 렘 부스터 프로그램(이하 부스터)은 게임 실행 파일이나 실행 중인 게임 데이터를 직접 수정하지 않습니다. 대신 Windows와 그래픽 드라이버 등이 기본으로 제공하는 설정을 이용해 마비노기가 조금 더 안정적으로 자원을 사용할 수 있는 환경을 만듭니다.

게임 화면이나 통신 내용을 조작하는 방식이 아니며, 적용한 CPU와 네트워크 설정을 다시 되돌릴 수 있는 공식적이고 일반적인 설정과 기능들을 활용합니다.

## Windows 기본 제공 기능 활용

### CPU 선호도 설정

컴퓨터의 CPU는 여러 작업을 동시에 처리합니다. 이때 마비노기와 다른 프로그램이 같은 CPU 영역을 계속 번갈아 사용하면 순간적인 끊김이 생길 수 있습니다.

부스터는 Windows가 제공하는 CPU 토폴로지에서 물리 코어, SMT 스레드와 성능 등급을 확인합니다. Intel 하이브리드 CPU에서는 P-core의 절반을 마비노기에 배정하고, 나머지 P-core와 모든 E-core를 다른 프로그램에 배정합니다. 터보 키와 주요 입력·매크로 엔진은 입력 응답성을 유지하면서 게임 연산을 방해하지 않도록 게임에 배정되지 않은 P-core만 사용합니다. P/E 구분이 없는 CPU도 물리 코어 단위로 절반을 나누며 같은 코어의 SMT 스레드는 함께 이동합니다. Windows 핵심 프로세스와 보안 프로그램처럼, 변경하면 안 되는 항목은 대상에서 제외합니다.

보통 자주 사용하시는 윈도우 기본 툴인 작업 관리자에서도 수동으로 설정할 수 있는 일반 기능입니다. 자세히 - 선호도 설정에서 프로세스 별 CPU 선호도를 수동으로도 변경할 수 있습니다.

![작업 관리자의 프로세서 선호도 설정 화면](./doc_operation/affinity.png)

보통은 Process Lasso 같은 프로그램을 통해 관리하시지만, 부스터는 마비노기에 좀 더 최적화된 선호도 구성 설정을 자동으로 적용합니다.


### 네트워크 응답 설정 최적화

부스터의 패스트핑 기능은 현재 인터넷 연결에 사용되는 네트워크 장치를 찾아 Windows의 응답 지연 관련 설정을 조정합니다. 작은 데이터가 불필요하게 대기하지 않도록 TcpAckFrequency와 TCPNoDelay 값을 사용합니다. 요즈음 거의 모든 밀레시안 분들이 사용하고 계시는 유명한 기능이며, 완전히 동일한 원리를 사용하고 있습니다.

TCP 자동 조정 기능은 Windows 권장 상태인 Normal로 맞춥니다. 추가로 네트워크 카드의 RSS 기능을 함께 적용하면 네트워크 처리 작업이 마비노기가 사용하는 CPU 영역과 겹치지 않도록 분리할 수도 있습니다(개발자 기능에서 제공될 예정).

### Windows 메모리 자동 정리

Windows는 빠른 재사용을 위해 사용하지 않는 데이터를 대기 메모리에 보관합니다. 보통은 유용하지만, 사용 가능한 메모리가 부족한 상황에서 대기 메모리가 많이 쌓이면 게임이 새 메모리를 확보하는 과정이 늦어질 수 있습니다.

부스터는 마비노기가 실행되는 동안 주기적으로 사용 가능한 메모리와 대기 메모리를 확인합니다. 사용 가능한 메모리가 실제로 부족하고 정리할 수 있는 대기 메모리가 충분할 때만 Windows 기능을 통해 대기 목록을 비웁니다. 이 기능은 흔히 사용하시는 ISLC 같은 프로그램과 완전히 동일한 원리입니다.

실행 중인 다른 프로그램의 메모리를 강제로 빼앗거나 종료하지 않습니다. 마찬가지로, 마비노기의 게임 메모리에 부적합하게 접근하는 일은 없습니다.


## 부스트 실시간 적용과 원상 복구 방식

### 프로세스 실행 상태 감시

부스터는 약 5초 간격으로 마비노기와 다른 프로그램의 실행 상태를 확인합니다. 마비노기를 나중에 실행하거나 새로운 프로그램이 켜져도 CPU 배치를 자동으로 다시 적용합니다.

이 감시는 프로그램 이름과 실행 상태를 확인해 Windows의 CPU 선호도를 조정할 뿐, 각 프로그램 내부의 데이터는 읽거나 변경하지 않습니다.

### 안전한 적용 복구

실시간 부스트를 일시정지하면 감시만 멈추고 이미 적용된 설정은 유지합니다. 완전히 복구하면 프로세스의 CPU 제한을 풀어 전체 CPU를 사용할 수 있게 합니다.

앱을 닫을 때 적용된 변경이 남아 있다면 복구 후 종료할지, 현재 상태를 유지할지 사용자가 직접 선택할 수 있습니다.


## 그래픽 드라이버 공식 API 활용

### NVIDIA 프로필 최적화

NVIDIA 그래픽카드에서는 마비노기 전용 드라이버 프로필을 찾아 수직 동기화를 끄고, 최대 프레임을 400 FPS로 설정합니다(프레임 제한이 없다면 오히려 마비노기 프로세스의 연산 효율이 떨어집니다). 스레드 최적화, 최고 성능 선호와 저지연 모드도 마비노기에 맞게 조정합니다.

다른 게임의 프로필은 건드리지 않습니다.

### Radeon 프로필 최적화

Radeon 그래픽카드에서는 AMD가 제공하는 공식 ADLX 기능을 이용합니다. 수직 동기화와 Enhanced Sync, Chill을 끄고 Anti-Lag를 켜 입력 지연과 불필요한 프레임 제한을 줄입니다.

현재 ADLX에서는 마비노기만을 위한 개별 프로필 변경이 제한적이므로 Radeon 설정은 그래픽카드 전체에 적용되고 앱을 종료해도 유지됩니다. 다른 게임에도 영향을 줄 수 있어 적용 전에 이 내용을 안내하고 있습니다. Radeon에서는 최대 프레임 400 FPS 설정을 적용하지 않습니다.


## 터보 키 기능과 마비노기 운영정책

마비노기는 반복 입력으로 정상 범위를 벗어난 게임 기록을 만들거나 반복 작업을 자동화하는 프로그램의 사용을 제한하고 있습니다.

마비노기 `권장하지 않는 플레이 방식 안내`
https://mabinogi.nexon.com/page/archive/guide_view.asp?id=4889849&num=7&playtarget=1

터보 키는 사용자가 키를 누르고 있는 동안 같은 입력을 연속으로 보내는 기능이므로 운영정책상 반복 입력 또는 매크로로 해석될 가능성이 있습니다. 양털 깎기·채집처럼 게임 재화에 영향을 주는 반복 작업, 다른 이용자와의 상호작용 경쟁, 정상적인 조작 범위를 벗어나는 기록 생성에 사용하면 제재 위험이 커질 수 있습니다.

터보 키는 여러 단계의 작업을 스스로 수행하지 않고 단일 키 입력만 반복하지만, 이러한 기능 범위만으로 운영정책상 허용된다고 단정할 수는 없습니다. 상용 키보드·마우스 프로그램에서 비슷한 기능을 제공하거나 다른 이용자가 사용하고 있다는 사실도 허용 여부를 보장하지 않습니다.

사용 전 최신 운영정책을 직접 확인하고, 게임 플레이를 자동화하거나 비정상적인 이득을 얻는 용도로 사용하지 마세요. 계정 제재 위험을 완전히 피하려면 터보 키를 사용하지 않는 것이 가장 안전합니다.